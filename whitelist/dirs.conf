# Yap — Directorios permitidos para abrir archivos (whitelist/dirs.conf)
# Formato: 1 ruta por linea; '~' se expande al HOME del usuario actual.
# Seguridad (HD-YAP-SEC-001): solo archivos regulares dentro de estas rutas
# pueden abrirse con xdg-open. Si el archivo no existe, fallback seguro:
# HOME + /etc/yap.
#
# Materiales del curso (guia PSeInt, apuntes)
/etc/yap
#
# Archivos personales del estudiante (tareas, documentos)
~