Correccion-54 (b510223) corrige tres fallos de CI/instalación. El merge lo cubre auto-merge.yml (auto-merge nativo de GitHub).

Objetivo
Que auto-merge y release funcionen sin mergear código no revisado, y que setup.sh lea el archivo VERSION.

Cambios
• fallback-merge.yml eliminado. Recorría todas las PRs abiertas contra main, aceptaba aprobaciones de un SHA viejo, ignoraba CHANGES_REQUESTED y además petaba con TypeError (checkRuns.filter sobre un objeto, no un array). Arreglar solo el TypeError habría activado los otros tres bugs. auto-merge.yml ya cubre el caso.
• auto-release.yml: import os junto a import re, para que el paso de CHANGELOG no falle con NameError.
• setup.sh: SCRIPT_DIR se define antes de leer $SCRIPT_DIR/VERSION (set -u ya no enmascara el fallo con el fallback 1.0.0).
• Tests de regresión en tests/test_yap_apparmor.py (orden de SCRIPT_DIR, workflow ausente, import os).
• README (línea de SCRIPT_DIR) y CHANGELOG.

Validación
python -m pytest tests/ — 162 passed. Repro de VERSION=2.5.0: YAP_VERSION leido: 2.5.0. El bloque Python de auto-release compila.

- [ ] La PR resuelve **un solo objetivo** (atomicidad adev). Si mezcla refactor/feature/docs, dividir.
- [ ] Rama corta basada en `main` (trunk-based, idealmente <48h), sin `force push` en la rama.
- [ ] El título usa **Conventional Commits**: `feat:`, `fix:`, `docs:`, `refactor:`, `chore:`, `test:`, `ci:`.
- [ ] Referencia el issue que resuelve: `Closes #N`.
- [ ] Validación ejecutada y evidenciada (check mínima que pruebe el cambio; en Yap: `python3 -m pytest tests/`).
- [ ] Sin secretos, credenciales ni datos personales en el diff.
- [ ] Contenido comunitario bilingüe cuando aplica (es primario / en mirror).



Closes #81
